{
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    // 这里的 paths 被删除，完全继承自 .nuxt/tsconfig.json
    // 仅保留非 paths 的配置（如果有需要的话，比如 strict 等，但通常基类已经配好了）
  },
  "include": [
    ".nuxt/nuxt.d.ts",
    "types/**/*.ts",
    "types/**/*.d.ts",
    "app/**/*.ts",
    "app/**/*.d.ts",
    "app/**/*.vue",
    "layers/**/*.ts",
    "layers/**/*.d.ts",
    "layers/**/*.vue"
  ],
  "exclude": ["node_modules"]
}
